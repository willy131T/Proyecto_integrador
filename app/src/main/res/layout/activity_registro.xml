<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F8F9FA"
    android:padding="24dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Registro de Paciente"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="#333333"
            android:textAlignment="center"
            android:layout_marginBottom="24dp"/>

        <!-- Nombre Completo -->
        <EditText
            android:id="@+id/etRegNombre"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:hint="Nombre completo"
            android:background="#FFFFFF"
            android:padding="12dp"
            android:layout_marginBottom="16dp"/>

        <!-- Usuario -->
        <EditText
            android:id="@+id/etRegUsuario"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:hint="Nombre de usuario"
            android:background="#FFFFFF"
            android:padding="12dp"
            android:layout_marginBottom="16dp"/>

        <!-- Contraseña -->
        <EditText
            android:id="@+id/etRegPassword"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:hint="Contraseña"
            android:inputType="textPassword"
            android:background="#FFFFFF"
            android:padding="12dp"
            android:layout_marginBottom="16dp"/>

        <!-- Edad -->
        <EditText
            android:id="@+id/etRegEdad"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:hint="Edad (ej. 20)"
            android:inputType="number"
            android:background="#FFFFFF"
            android:padding="12dp"
            android:layout_marginBottom="16dp"/>

        <!-- Alergias -->
        <EditText
            android:id="@+id/etRegAlergias"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:hint="Alergias (si no tienes, escribe Ninguna)"
            android:background="#FFFFFF"
            android:padding="12dp"
            android:layout_marginBottom="24dp"/>

        <Button
            android:id="@+id/btnRegistrarseFinal"
            android:layout_width="match_parent"
            android:layout_height="55dp"
            android:text="REGISTRARSE"
            android:textSize="16sp"
            android:backgroundTint="#0056b3"/>

    </LinearLayout>
</ScrollView>